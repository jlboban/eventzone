{% extends 'home/base.html.twig' %}

{% block title %}Venues{% endblock %}

{% block body %}

<!-- Content wrapper start -->
<div class="content-wrapper">
    <div class="container-fluid">
        <div class="row">
            {% for venue in venues %}
                {% include 'venue/_venue_card.html.twig' %}
            {% endfor %}
        </div>
    </div>
</div>
<!-- Content wrapper end -->

{% endblock %}
