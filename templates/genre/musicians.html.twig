{% extends 'home/base.html.twig' %}

{% block title %}Musicians{% endblock %}

{% block body %}

<!-- Content wrapper start -->
<div class="content-wrapper">
    <div class="container-fluid">
            <div class="row">

                {% for genre in genres %}
                    {% for musician in genre.musicians %}
                    {% include 'musician/_musician_card.html.twig' %}
                    {% endfor %}
                {% endfor %}

            </div>
        </div>
    </div>
<!-- Content wrapper end -->

{% endblock %}
