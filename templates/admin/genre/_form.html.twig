<div class="container-fluid">
    <div class="row row-eq-spacing-lg">
        <div class="col-lg-3"></div>
        <div class="col-lg-6">

            <div class="content">
                {{ form_start(form) }}
                    {{ form_widget(form) }}

                    <button class="btn btn-event w-full">{{ button_label|default('Save') }}</button>

                {{ form_end(form) }}
            </div>

        </div>
        <div class="col-lg-3"></div>
    </div>
</div>
